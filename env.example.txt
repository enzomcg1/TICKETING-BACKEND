# ============================================
# CONFIGURACIÓN DE BASE DE DATOS
# ============================================
# URL de conexión a PostgreSQL
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_base_datos?schema=public
DATABASE_URL=postgresql://postgres:root123@localhost:5432/TicketDemo?schema=public

# ============================================
# CONFIGURACIÓN DEL SERVIDOR
# ============================================
# Puerto en el que el servidor escuchará (por defecto: 3000)
PORT=3000

# Entorno de ejecución (development | production)
NODE_ENV=development

# ============================================
# CONFIGURACIÓN DE AUTENTICACIÓN JWT
# ============================================
# Secreto para firmar tokens JWT (¡CAMBIAR EN PRODUCCIÓN!)
# Generar un string aleatorio seguro, por ejemplo: openssl rand -base64 32
JWT_SECRET=ticketing_system_secret_key_2024_change_in_production

# Tiempo de expiración del token JWT (por defecto: 7d)
# Formatos válidos: "1h", "24h", "7d", "30d", etc.
JWT_EXPIRES_IN=7d

# ============================================
# CONFIGURACIÓN DE EMAIL (NOTIFICACIONES)
# ============================================
# Email desde el que se enviarán las notificaciones
# Soporta Gmail, Outlook/Hotmail (auto-detecta según dominio)
EMAIL_USER=pruebaticket4@gmail.com

# App Password del email (NO usar la contraseña normal)
# Para Gmail: https://myaccount.google.com/apppasswords
# Para Outlook: https://account.microsoft.com/security (App Passwords)
EMAIL_PASSWORD=tu_app_password_aqui

# Habilitar/deshabilitar notificaciones por email (true | false)
ENABLE_EMAIL_NOTIFICATIONS=true

# Configuración SMTP genérica (solo si usas otro proveedor)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Email de prueba para testing (opcional)
TEST_EMAIL=tu-email@ejemplo.com

# ============================================
# CONFIGURACIÓN DE SOCKET.IO (NOTIFICACIONES EN TIEMPO REAL)
# ============================================
# Habilitar/deshabilitar notificaciones WebSocket (true | false)
# Por defecto: true (solo escribir 'false' para deshabilitar)
ENABLE_SOCKET_NOTIFICATIONS=true

# ============================================
# CONFIGURACIÓN DEL FRONTEND (CORS)
# ============================================
# URL del frontend (puede ser múltiple separado por comas)
# Ejemplos:
#   - Desarrollo local: http://localhost:5173
#   - Red local: http://192.168.1.100:5173,http://192.168.1.101:5173
#   - Producción: https://tickets.tudominio.com
#   - Múltiples orígenes: http://localhost:5173,https://app.tudominio.com
FRONTEND_URL=http://localhost:5173

# ============================================
# CONFIGURACIÓN DE ALMACENAMIENTO DE ARCHIVOS
# ============================================
# Tipo de almacenamiento (local | s3 | cloudinary)
# Por ahora solo 'local' está implementado
STORAGE_TYPE=local

# Directorio donde se guardarán los archivos subidos (relativo a la raíz del backend)
# Por defecto: 'uploads'
UPLOAD_DIR=uploads

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. Renombrar este archivo a .env y llenar con tus valores reales:
#    Rename-Item env.example.txt .env
#
# 2. NUNCA subir el archivo .env al repositorio (está en .gitignore)
#
# 3. En producción:
#    - Cambiar JWT_SECRET por un valor seguro y único
#    - Cambiar NODE_ENV a 'production'
#    - Usar variables de entorno del sistema o un gestor de secretos
#    - Configurar FRONTEND_URL con el dominio real
#
# 4. Para Gmail:
#    - Habilitar verificación en 2 pasos
#    - Generar App Password desde: https://myaccount.google.com/apppasswords
#    - Usar el App Password (16 caracteres sin espacios) en EMAIL_PASSWORD
#
# 5. Para Outlook:
#    - Microsoft ha deshabilitado autenticación básica en muchas cuentas
#    - Se recomienda usar Gmail o un servicio de terceros (SendGrid, etc.)
#    - Si usas Outlook, genera App Password desde: https://account.microsoft.com/security
